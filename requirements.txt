# Python dependencies for the catalog build scripts (scripts/*.py).
# Used by GitHub Actions workflows and by nixpacks (Railway) for the /cron/build webhook.
ncoreparser>=4.5.0
requests>=2.31.0
python-dotenv>=1.0.0
